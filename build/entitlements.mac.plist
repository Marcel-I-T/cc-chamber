<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Electron uses V8 JIT -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Electron renderer wants unsigned executable memory -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- node-pty and other native modules are unsigned -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Allow inheriting DYLD_* env when spawning helpers -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- We spawn `claude` which may launch many child processes -->
    <key>com.apple.security.inherit</key>
    <true/>
  </dict>
</plist>
